# ═══════════════ TMUX JADOO ═══════════════
set -g mouse on
set -g history-limit 99999
set -g status-style bg=#1e1e2e,fg=#89b4fa
set -g pane-border-style fg=#313244
set -g pane-active-border-style fg=#89b4fa

unbind C-b
set -g prefix C-a
bind C-a send-prefix

bind c new-window -c "#{pane_current_path}"
bind % split-window -h -c "#{pane_current_path}"
bind '"' split-window -v -c "#{pane_current_path}"
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

bind -n M-F11 resize-pane -Z
set -g mode-keys vi
bind -T copy-mode-vi v send -X begin-selection
bind -T copy-mode-vi y send -X copy-pipe-and-cancel "xclip -in -selection clipboard"

# پلاگین‌ها (خودکار نصب می‌شن)
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'tmux-plugins/tmux-yank'
set -g @continuum-restore 'on'

bind C new-session -c ~ \; rename-session 🏠
bind D new-session -c ~/Downloads \; rename-session 📥
bind P new-session -c ~/projects \; rename-session 💻

run '~/.tmux/plugins/tpm/tpm'
# ════════════════════════════════════════
